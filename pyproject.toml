[project]
name = "proyecto-tajamar"
version = "0.1.0"
description = "Lista de gastos"
authors = [
    {name = "Manish Soneji",email = "manish.soneji@hotmail.com"}
]
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "mypy (>=2.1.0,<3.0.0)",
    "ruff (>=0.15.15,<0.16.0)",
    "pre-commit (>=4.6.0,<5.0.0)",
    "bandit (>=1.9.4,<2.0.0)",
    "types-tabulate (>=0.10.0.20260508,<0.11.0.0)"
]
